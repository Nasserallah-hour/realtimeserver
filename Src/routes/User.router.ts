// import express from "express";
// import UserController from "../../controllers/common-module/user.controller";

// export default class UserRouter {
//   constructor(
//     private prefix: string,
//     private middleware : any,
//     private router: express.Router,
//     private controller: UserController
//   ) {
//     router.post(prefix + "/login",middleware, controller.LoginUser);
//     router.post(prefix + "/activate",middleware, controller.ActivateUser);
//     router.post(prefix + "/new", middleware,controller.CreateUser);
//     router.put(prefix + "/update", middleware,controller.UpdateUser);
//     router.get(prefix + "/:id", middleware,controller.GetUser);
//     router.get(prefix, middleware,controller.GetAllUsers);
//     router.get(prefix + "/test",middleware, controller.TestUser);
//   }
// }
